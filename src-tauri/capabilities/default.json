{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the page may ask of the shell, wherever it runs",
  "windows": ["main"],
  "permissions": ["core:default"]
}
